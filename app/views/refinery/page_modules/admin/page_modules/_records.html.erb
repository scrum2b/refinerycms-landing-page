<%= render 'refinery/admin/search_header', :url => refinery.page_modules_admin_page_modules_path %>
<div class='pagination_container'>
  <% if @page_modules.any? %>
    <%= render 'page_modules' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
